version: "2"

options:
  compose-file-path: 'docker-compose.yml'
  compose-dev-file-path: 'docker-compose.dev.yml'
  verbose: true

syncs:
  v-sync-mash-node_modules:
    # Seems to be the path needs to be relative from current directory
    src: './node_modules'
    notify_terminal: true
