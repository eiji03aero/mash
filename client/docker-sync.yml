version: "2"

options:
  compose-file-path: 'docker-compose.yml'
  compose-dev-file-path: 'docker-compose.dev.yml'
  verbose: true

syncs:
  v-sync-mash:
    # Seems to be the path needs to be relative from current working directory
    src: '.'
    notify_terminal: true
